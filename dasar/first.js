console.log('Hello world');

function myMessage (name) {
    // func dapat di define menggunakan expresi const, let, var
    // cons myMessage = (name) => {}

    console.log(`Hello ${name}, printed using myMessage func`);
    /* 
    gunakan `` untuk membuat format string seperti fstring di python
    dan tambahakn $ sebelum brackets
    */
} 
myMessage('Zakky');
